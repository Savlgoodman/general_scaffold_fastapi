# Normalize text files in the repository to LF.
* text=auto eol=lf

# Keep Windows-native scripts with CRLF for better shell compatibility.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
